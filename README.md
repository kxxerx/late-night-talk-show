# 오염도 상점 사이트 스타터

GitHub Pages + Supabase로 만드는 반자동 오염도/유쾌주화/상점 시스템입니다.

## 전체 구조

- GitHub Pages: 화면 표시용 HTML/CSS/JavaScript
- Supabase: 회원가입, 로그인, 데이터베이스, 권한관리
- 실제 현금 결제 없음
- 밴드 API 연동 없음
- 사이트 아이디와 밴드 닉네임은 관리자가 수동 연결

## 파일 구성

```txt
pollution-shop-starter/
├─ index.html              # 내 상태 / 메인
├─ login.html              # 회원가입 / 로그인
├─ shop.html               # 상점
├─ inventory.html          # 내 가방 / 아이템 사용
├─ codes.html              # 이벤트 코드 제출 / 내 제출내역
├─ admin.html              # 관리자 페이지
├─ css/
│  └─ style.css
├─ js/
│  ├─ config.example.js    # Supabase 설정 예시
│  ├─ supabaseClient.js
│  ├─ common.js
│  ├─ login.js
│  ├─ dashboard.js
│  ├─ shop.js
│  ├─ inventory.js
│  ├─ codes.js
│  └─ admin.js
└─ supabase/
   └─ setup.sql            # Supabase SQL Editor에 붙여넣을 DB 코드
```

---

# 1. Supabase 만들기

1. Supabase에 가입합니다.
2. 새 Project를 만듭니다.
3. Project가 생성되면 왼쪽 메뉴에서 `SQL Editor`로 들어갑니다.
4. `New query`를 누릅니다.
5. `supabase/setup.sql` 파일 안의 내용을 전부 복사해서 붙여넣습니다.
6. `Run`을 누릅니다.

---

# 2. Supabase URL / anon key 확인

1. Supabase 왼쪽 아래 톱니바퀴 `Project Settings`로 들어갑니다.
2. `Data API` 또는 `API` 메뉴로 들어갑니다.
3. 아래 두 값을 찾습니다.

- Project URL
- anon public key

---

# 3. config.js 만들기

`js/config.example.js` 파일을 복사해서 같은 폴더에 `config.js`라는 이름으로 만듭니다.

그리고 안의 값을 본인 Supabase 값으로 바꿉니다.

```js
export const SUPABASE_URL = "여기에 Project URL";
export const SUPABASE_ANON_KEY = "여기에 anon public key";
```

주의: `service_role key`는 절대 넣지 마세요. 이 키를 넣으면 사이트를 보는 사람이 관리자 열쇠를 훔칠 수 있습니다. 인간 불신은 시스템 설계의 기본입니다.

---

# 4. GitHub에 올리기

1. GitHub에서 새 repository를 만듭니다.
2. 이 폴더 안의 파일을 전부 업로드합니다.
3. 반드시 `js/config.js`도 포함되어 있어야 합니다.
4. Settings → Pages로 들어갑니다.
5. Source를 `Deploy from a branch`로 설정합니다.
6. Branch는 `main`, folder는 `/root`로 설정합니다.
7. 저장하면 잠시 후 사이트 주소가 생깁니다.

---

# 5. 첫 관리자 지정하기

1. 사이트에서 먼저 회원가입을 합니다.
2. Supabase → SQL Editor → New query에 아래 코드를 입력합니다.
3. 이메일 주소를 본인 가입 이메일로 바꿉니다.

```sql
update public.profiles
set role = 'admin'
where email = '본인메일@example.com';
```

4. Run을 누릅니다.
5. 다시 사이트에 로그인하면 관리자 페이지를 쓸 수 있습니다.

---

# 6. 사용 순서

## 사용자

1. 회원가입
2. 로그인
3. 내 상태 확인
4. 상점에서 아이템 구매
5. 내 가방에서 아이템 사용
6. 이벤트 코드 제출

## 관리자

1. 관리자 페이지 접속
2. 회원 검색
3. 밴드 닉네임 확인/수동 연결
4. 유쾌주화/오염도 수동 조정
5. 아이템 등록/수정
6. 이벤트 코드 생성
7. 제출된 이벤트 코드 승인/거절
8. 여러 회원 일괄 지급

---

# 7. 지금 버전의 아이템 효과

현재 자동 사용 가능한 효과는 아래 1개입니다.

```txt
effect_type = pollution_delta
effect_value = -10
```

예시: 오염도 -10

나중에 기간제 효과, 자동 방어 효과, 랜덤 효과를 붙일 수 있지만 첫 버전에서 넣으면 코드가 마계로 갑니다.

---

# 8. 오류가 나면 보여줄 것

오류가 나면 아래 중 하나를 캡처하거나 복사해서 보여주세요.

- 브라우저 화면의 오류 문구
- 개발자도구 Console 탭의 빨간 오류
- Supabase SQL Editor 오류
- GitHub Pages 배포 오류

개발자도구 여는 법:

- 크롬에서 `F12`
- 또는 마우스 오른쪽 클릭 → 검사 → Console

---

# 9. 중요한 제한

이 사이트는 학습/커뮤니티용 스타터입니다.

- 실제 현금 거래 금지
- 상품권/기프티콘 교환 기능 없음
- 회원 간 거래 기능 없음
- 밴드 API 자동 연동 없음
- 관리자 권한은 Supabase DB 정책으로 보호

---

# Supabase 새 프로젝트 생성 시 Security 체크

프로젝트 생성 화면에서 Security 관련 항목이 나오면 아래처럼 설정하세요.

```txt
Enable Data API → ON
Automatically expose new tables → OFF
Enable automatic RLS → ON
```

이 버전의 `supabase/setup.sql`에는 `Automatically expose new tables`를 꺼둔 상태에서도 작동하도록 수동 권한 부여 SQL이 포함되어 있습니다.

따라서 별도로 grant SQL을 따로 실행하지 않아도 됩니다.  
`setup.sql` 전체를 한 번에 실행하면 됩니다.

---

# 회원가입 이메일 확인 끄기

이 프로젝트는 폐쇄형 커뮤니티/밴드 수동 확인용으로 설계되어 있습니다.  
초기 운영에서는 Supabase 이메일 확인을 꺼두는 편이 사용자가 덜 헷갈립니다.

경로는 보통 아래 중 하나입니다.

```txt
Authentication
→ Sign In / Providers
→ Email
→ Confirm email OFF
→ Save
```

또는

```txt
Authentication
→ Providers
→ Email
→ Confirm email OFF
→ Save
```

이 설정을 끄면 새 회원은 이메일 확인 없이 가입 후 바로 로그인할 수 있습니다.

v0.3부터 `js/login.js`는 회원가입 후 자동 로그인을 시도합니다.  
만약 Supabase에서 Confirm email이 켜져 있으면 자동 로그인되지 않고 이메일 확인 안내가 표시됩니다.

---

# v0.4 변경사항

## 1. 기본 화면 변경

기본 `index.html`은 이제 내 상태 화면이 아니라 상점 화면입니다.  
오른쪽에는 프로필 카드가 표시됩니다.

## 2. 내 상태 화면 분리

내 상태 화면은 `mypage.html`로 분리되었습니다.

## 3. 아이디/비밀번호 가입

사용자는 이메일 대신 아이디와 비밀번호로 가입합니다.  
단, Supabase Auth는 내부적으로 이메일 형식이 필요하므로 코드가 자동으로 아래 형태의 내부 이메일을 만듭니다.

```txt
아이디@pollution.invalid
```

사용자는 이 내부 이메일을 몰라도 됩니다.

## 4. 탈퇴 처리

`mypage.html`에 탈퇴 버튼이 추가되었습니다.  
현재 탈퇴는 Supabase Auth 사용자를 완전히 삭제하는 기능이 아니라, 프로필을 `withdrawn` 상태로 바꾸고 유쾌주화/오염도/아이템을 초기화하는 비활성화 방식입니다.

완전 삭제가 필요하면 관리자가 Supabase Dashboard의 Authentication → Users에서 해당 사용자를 직접 삭제해야 합니다.

## 5. 이미 설치한 프로젝트라면

이미 v0.3 이하의 `setup.sql`을 실행했다면, Supabase SQL Editor에서 아래 파일을 추가로 실행하세요.

```txt
migrations/upgrade-v0.4.sql
```

새로 처음 설치하는 경우에는 `supabase/setup.sql`만 실행하면 됩니다.

## 6. 디자인 수정 방법

대부분의 화면 스타일은 아래 파일에서 바꿉니다.

```txt
css/style.css
```

친구가 디자인을 해준다면 보통 이 파일을 수정하면 됩니다.  
화면 구조 자체를 바꾸려면 HTML 파일을 수정해야 합니다.

```txt
index.html       상점 메인
mypage.html      내 상태
inventory.html   내 가방
codes.html       이벤트 코드
login.html       로그인/회원가입
admin.html       관리자
```

---

# v0.5 변경사항

## 1. 상점 첫 화면 헤더 변경

`index.html`과 `shop.html`에서 큰 "상점" 제목을 제거하고, 블로그형 상단 배너 영역을 추가했습니다.

로고/이미지 영역은 아래 파일을 교체하면 됩니다.

```txt
assets/site-logo.svg
```

원하는 이미지가 있다면 같은 파일명으로 교체하거나, `index.html`의 아래 부분을 바꾸면 됩니다.

```html
<img src="assets/site-logo.svg" alt="사이트 로고">
```

## 2. 상점 카테고리

오른쪽 프로필 영역 아래에 상점 카테고리를 추가했습니다.

```txt
전체
메인 상점 아이템
정화 아이템
이벤트 아이템
특수 아이템
```

DB의 `items.category` 값으로 분류합니다.

```txt
main
cleanse
event
special
```

## 3. 프로필 사진 파일 업로드

Supabase Storage의 `avatars` 버킷을 사용합니다.  
이미 설치한 프로젝트라면 Supabase SQL Editor에서 아래 파일을 실행하세요.

```txt
migrations/upgrade-v0.5.sql
```

그 뒤 `mypage.html`에서 이미지 파일을 업로드할 수 있습니다.

## 4. 이미 v0.4를 적용했다면

아래 순서로 진행하세요.

```txt
1. Supabase SQL Editor에서 migrations/upgrade-v0.5.sql 실행
2. GitHub에 v0.5 파일 업로드
3. GitHub Pages 새로고침
```

---

# v0.6 변경사항

## 1. 상점 공개 진입

이제 로그인하지 않아도 `index.html` 상점 화면이 먼저 보입니다.

## 2. 비로그인 상태 구매 제한

비로그인 상태에서 `구입하기` 버튼을 누르면 구매가 진행되지 않고 오른쪽 로그인 영역을 안내합니다.

## 3. 오른쪽 사이드 영역 변경

로그인 전에는 오른쪽 사이드 영역에 로그인 폼과 회원가입 폼이 표시됩니다.

로그인 후에는 기존처럼 프로필 카드, 내 가방, 내 상태, 코드 제출 버튼이 표시됩니다.

## 4. DB 변경 없음

v0.5까지 적용했다면 v0.6은 추가 SQL 실행이 필요 없습니다.  
파일만 GitHub에 업로드하면 됩니다.

---

# v0.7 변경사항

## 1. 공통 상단 구조 정리

모든 주요 페이지에 같은 상단 로고/메뉴 구조를 적용했습니다.

```txt
로고 클릭 → 홈/상점
상단 메뉴 → 상점 / 내 정보 / 내 가방 / 이벤트 코드 / 관리자
```

## 2. 상점 카테고리 위치 변경

상점 카테고리는 더 이상 프로필 아래에 붙지 않습니다.  
로고와 상점 아이템 사이의 가로 메뉴로 이동했습니다.

## 3. 사이트 아이디 숨김

사용자 화면에서 사이트 아이디 노출을 줄였습니다.  
로그인/운영 내부 식별자로만 사용합니다.

## 4. 이벤트 코드 간소화

증빙 입력란을 제거했습니다.  
사용자는 이벤트 코드만 입력합니다.

## 5. 폰트와 카드 크기 정리

폰트 크기를 전반적으로 줄이고 통일했습니다.  
상점 아이템 카드는 한 줄에 3~4개 정도 들어가도록 작게 조정했습니다.

## 6. 추가 SQL 없음

v0.5까지 적용했다면 v0.7은 추가 SQL 실행이 필요 없습니다.  
GitHub 파일만 업로드하면 됩니다.

---

# v0.8 변경사항

## 1. 오류 수정

- 내 가방 오류 수정: `#myPollution` 요소를 찾지 못하던 문제 제거
- 이벤트 코드 오류 수정: `created_at` 대신 실제 컬럼인 `submitted_at` 사용
- 관리자 오류 수정: `event_submissions`와 `profiles` 사이의 관계가 여러 개라 생기던 embed 오류 수정

## 2. 상단 메뉴 제거

`상점 / 내 정보 / 내 가방 / 이벤트 코드 / 관리자` 메뉴를 상단에서 제거했습니다.  
관리자 링크는 관리자 계정으로 로그인했을 때 상점 오른쪽 프로필 카드에만 표시됩니다.

## 3. 카테고리 버튼 정리

상점 카테고리 버튼을 단색의 단순한 버튼으로 바꿨습니다.

## 4. 아이템 카드 축소

상점 아이템 카드와 이미지 크기를 더 강하게 줄였습니다.  
PC 기준 한 줄 4개 정도가 보이도록 조정했습니다.

## 5. 추가 SQL 없음

v0.5까지 적용했다면 v0.8은 추가 SQL 실행이 필요 없습니다.  
GitHub 파일만 업로드하면 됩니다.

---

# v0.9 변경사항

## 1. 마스코트 골든의 기념품샵 테마

전체 색감을 파스텔/솜사탕/기념품샵 느낌으로 조정했습니다.

## 2. 메뉴명 변경

- 내 정보 → 손님 카드
- 내 가방 → 선물 주머니
- 이벤트 코드 → 초대장 코드
- 관리자 → 백룸 관리
- 로그아웃 → 기념품샵에서 나가기

`기념품샵에서 나가기`를 누르면 확인창이 표시됩니다.

```txt
정말 기념품샵에서 나가시겠습니까? 완전히 소각당하시겠습니까?
```

## 3. 제휴 페이지 추가

`partner.html`이 추가되었습니다.  
로그인한 손님만 접근할 수 있고, `심야 토크쇼 초대장`을 열어 밴드로 이동할 수 있습니다.

밴드 주소는 `js/config.js`에 아래 줄을 추가해 설정합니다.

```js
export const BAND_INVITE_URL = "https://band.us/...";
```

## 4. 관리자 탈퇴 회원 삭제

탈퇴 처리된 회원을 관리자 페이지에서 사이트 DB 기준으로 삭제할 수 있습니다.

단, Supabase Auth의 실제 인증 계정은 브라우저에서 안전하게 완전 삭제할 수 없습니다.  
완전 삭제가 필요하면 Supabase Dashboard → Authentication → Users에서 별도로 삭제해야 합니다.

## 5. 마스코트 골든의 기념품샵 기본 아이템

기존 기본 아이템 이름을 기념품샵 분위기로 바꾸고, 몇 가지 기본 선물을 추가합니다.

이미 v0.8까지 적용했다면 Supabase SQL Editor에서 아래 파일을 실행하세요.

```txt
migrations/upgrade-v0.9.sql
```

---

# v1.0 변경사항

## 확정 명칭 반영

사이트명:

```txt
마스코트 골든의 기념품샵
심야 토크쇼 특별관
```

메뉴:

```txt
특별관 / 쇼핑백 / 방문객 정보 / 초대권 등록 / 관리실
```

용어:

```txt
오염도 → 방문객 상태
이벤트 코드 → 초대권 등록
아이템 카테고리 → 기념품 / 분실물 / 특별 상품
로그아웃 → 다음에 다시 방문하기
탈퇴 → 기념품샵에서 나가기
```

## 기념품샵에서 나가기 공포 모달

방문객 정보 페이지의 `기념품샵에서 나가기` 버튼을 누르면 검은 배경과 빨간 글씨의 커스텀 모달이 표시됩니다.

```txt
완전히 소각당하시겠습니까?
```

아래에는 작은 빨간 글씨로 `나가지마` 문구가 반복됩니다.

## 추가 SQL 없음

v0.9까지 적용했다면 v1.0은 추가 SQL 실행이 필요 없습니다.  
GitHub 파일만 업로드하면 됩니다.

---

# v1.1 변경사항

## 1. 로고/상단 색상 변경

상단 로고 영역을 검은색 + 금색 계열로 변경했습니다.  
로고 문구도 아래처럼 명확히 들어갑니다.

```txt
마스코트 골든의 기념품샵
심야 토크쇼 특별관
```

## 2. 상단 메뉴 제거

`특별관 / 쇼핑백 / 방문객 정보 / 초대권 등록 / 관리실` 형태의 상단 메뉴를 제거했습니다.  
대신 상점 카테고리 줄에 `특별관` 링크를 추가했습니다.

```txt
특별관 / 전체 / 기념품 / 분실물 / 특별 상품
```

## 3. 오른쪽 프로필 버튼 정리

오른쪽 프로필 영역 버튼은 아래 명칭으로 정리했습니다.

```txt
쇼핑백
방문객 정보
초대권 등록
특별관
관리실
다시 방문하기
```

로그아웃 문구는 `다시 방문하기`로 줄였습니다.

## 4. 기념품샵 나가기 영역 정리

방문객 정보 페이지의 탈퇴 영역 설명을 정리하고, 버튼 문구를 `기념품샵에서 나가기`로 고정했습니다.

## 5. 공포 모달 수정

`기념품샵에서 나가기` 클릭 시 뜨는 검은색/빨간색 공포 모달의 색상과 버튼 작동을 수정했습니다.

## 6. 추가 SQL 없음

v0.9까지 적용했다면 v1.1은 추가 SQL 실행이 필요 없습니다.

---

# v1.2 변경사항

## 1. 로고 문구 변경

로고 문구를 아래 하나로 정리했습니다.

```txt
마스코트 골든의 기념품샵
```

`심야 토크쇼 특별관` 문구는 로고에서 제거했습니다.

## 2. 카테고리형 메뉴를 전체 페이지에 표시

아래 카테고리형 메뉴가 모든 주요 페이지 상단에 표시됩니다.

```txt
전체 / 기념품 / 분실물 / 특별 상품 / 특별관
```

단, `특별관`은 로그인한 방문객에게만 표시됩니다.

## 3. 특별관 문구 교체

특별관 페이지 문구를 새 텍스트로 교체했습니다.

`우 리 같 이 놀 자`는 노란색 볼드체로 표시됩니다.  
그 아래 긴 문장은 이탤릭체 + 가비아 마음결체로 표시됩니다.

## 4. 웹폰트

가비아 마음결체는 웹폰트 CDN을 통해 불러옵니다.  
네트워크 상태나 CDN 문제로 불러오지 못하면 기본 serif 계열 폰트로 대체됩니다.

## 5. 추가 SQL 없음

v0.9까지 적용했다면 v1.2는 추가 SQL 실행이 필요 없습니다.

---

# v1.3 변경사항

## 1. 기념품샵 용어 반영

`기프트샵` 표현을 `기념품샵`으로 바꿨습니다.

## 2. 파스텔 UI 톤 조정

첨부 이미지처럼 연분홍/연보라/민트/하늘색 계열의 뽀용한 배경 톤을 추가했습니다.

## 3. 오른쪽 프로필 버튼 정리

오른쪽 프로필 영역에서 `특별관` 버튼을 제거했습니다. 특별관은 상단 카테고리형 메뉴에서만 접근합니다.

## 4. 유쾌주화 명칭 반영

`재화`, `조각` 표현을 `유쾌주화`로 바꿨습니다.

## 5. 방문객 상태 판정 표시

방문객 상태 수치 옆에 안정/불안정 등 판정이 다시 보이도록 했습니다.

## 6. 회원가입 팝업화

오른쪽 로그인 영역의 방문객 등록은 접히는 영역이 아니라 팝업 모달로 뜹니다.

## 7. 기념품샵에서 나가기 팝업 수정

기념품샵에서 나가기 공포 팝업을 고정 팝업 방식으로 다시 만들고, 버튼 동작을 강화했습니다.

## 8. 추가 SQL 없음

v0.9까지 적용했다면 v1.3은 추가 SQL 실행이 필요 없습니다.

---

# v1.4 변경사항

## 1. 초대장 화면 수정

초대장 화면을 검은 배경 + 빨간 기본 글씨로 변경했습니다. `우 리 같 이 놀 자`는 노란색 볼드체, 그 아래 문장은 가비아 마음결체 + 이탤릭체로 표시합니다. `초대장 받기` 버튼은 카드 하단 중앙에 배치했습니다.

## 2. 쇼핑백 빈 상태 박스

보유 아이템이 없을 때도 둥근 네모 박스 안에 `보유 아이템이 없습니다.`가 표시됩니다.

## 3. 프로필 표시 정리

프로필 카드에서 이름 아래의 판정 텍스트를 제거하고, 유쾌주화/방문객 상태/판정이 세 칸으로 나란히 보이도록 정리했습니다.

## 4. 방문객 등록 팝업 수정

방문객 등록은 아래로 펼쳐지지 않고 팝업 모달로 열립니다. 등록 완료 후 자동으로 닫힙니다.

## 5. 사용자 이름 처리

회원가입 시 사용자 이름은 기본적으로 `익명`으로 저장됩니다. 방문객 정보 페이지의 사용자 이름은 읽기 전용이며, 관리자가 수정할 수 있습니다. 프로필에는 밴드 닉네임이 있으면 밴드 닉네임을 우선 표시하고, 없으면 사용자 이름 또는 `익명`을 표시합니다.

## 6. 기념품샵 나가기 팝업 수정

기념품샵에서 나가기 버튼이 다시 작동하도록 공포 팝업 동작을 JS에서 강하게 재작성했습니다.

## 7. 추가 SQL 없음

v0.9까지 적용했다면 v1.4는 추가 SQL 실행이 필요 없습니다.

---

# v1.5 변경사항

## 1. 비로그인 상점 공개 권한 수정

탈퇴/로그아웃 후에도 비로그인 상태에서 활성 아이템 목록을 볼 수 있도록 `items` 공개 읽기 권한을 추가했습니다.

## 2. 유쾌주화 부족 메시지

구입 시 잔액이 부족하면 `유쾌주화가 부족합니다.`라고 표시됩니다.

## 3. 관리자 방문객 제거

관리실에서 탈퇴하지 않은 방문객 정보도 사이트 DB 기준으로 제거할 수 있습니다. 비밀번호를 잊은 방문객이 같은 아이디로 다시 등록해야 할 때 사용할 수 있습니다.

단, Supabase Auth 실제 계정은 별도로 남을 수 있습니다.

## 4. 비밀번호 분실 안내

방문객 입장 영역에 `비밀번호를 잊으셨나요?` 안내 팝업을 추가했습니다. 현재 구조상 자동 이메일 복구는 사용할 수 없으므로 관리실 문의 방식으로 안내합니다.

## 5. 프로필 상태 표시

프로필 카드의 세 칸을 두 칸으로 줄이고, `판정 상태` 칸에 오염도 숫자와 안정/불안정 판정을 함께 표시합니다.

## 6. 초대장 스타일 조정

`우 리 같 이 놀 자` 글자 크기를 줄이고, 가비아 마음결체 문장을 흰색으로 바꿨습니다. `초대장 받기` 버튼도 검은 바탕+빨간 글씨로 맞췄습니다.

## 7. v1.5 SQL 필요

Supabase SQL Editor에서 아래 파일을 실행해야 합니다.

```txt
migrations/upgrade-v1.5.sql
```

---

# v1.6 변경사항

## 1. 방문객 상태 판정표 변경

```txt
0~20   안정
21~40  경미한 오염
41~70  불안정
71~99  위험
100    침식 · 우리가 된 걸 환영해
```

프로필 카드에서는 `판정 상태 · 안정`처럼 보이지 않고, `안정`, `경미한 오염` 같은 판정명만 표시됩니다.

## 2. 초대장 보상

특별관에서 `초대장 받기`를 누르면 최초 1회에 한해 다음 보상이 지급됩니다.

```txt
유쾌주화 +10
방문객 상태 +10
```

이미 받은 초대장을 다시 열면 보상은 중복 지급되지 않습니다.

## 3. J3/괴이 방문객 유형

관리실에서 방문객 유형을 `일반` 또는 `J3/괴이`로 지정할 수 있습니다. `J3/괴이` 유형은 방문객 상태가 `측정 불필요`로 표시되고, 초대장을 받아도 방문객 상태가 오르지 않습니다. 유쾌주화 보상은 지급됩니다.

## 4. v1.6 SQL 필요

Supabase SQL Editor에서 아래 파일을 실행해야 합니다.

```txt
migrations/upgrade-v1.6.sql
```

---

# v1.7 변경사항

## 1. 초대권 등록 문구 수정

초대권 등록 페이지 설명을 아래 문구로 바꿨습니다.

```txt
사회자님이 안내한 코드를 입력하세요. 승인이 필요한 코드는 사회자님의 확인 후 반영됩니다.
```

`초대권 등록 입력`은 `초대권 코드 입력`으로 바꾸고, 입력칸 라벨의 `초대권 등록` 문구는 제거했습니다.

## 2. 방문객 상태 문구 변경

```txt
0~20    괜 찮 아
21~40   괜 찮 아 ?
41~70   조 심 해
71~99   위 험 해
100     안 돼 돌 아 가
```

100 미만은 노란색 볼드, 100은 빨간색 볼드로 표시됩니다.

## 3. 비밀번호 분실 요청 관리

방문객이 비밀번호 분실 요청을 보내면 관리실의 `비밀번호 분실 요청` 목록에서 확인할 수 있습니다.

## 4. v1.7 SQL 필요

Supabase SQL Editor에서 아래 파일을 실행해야 합니다.

```txt
migrations/upgrade-v1.7.sql
```

---

# v1.8 변경사항

## 1. 초대권 코드 관리

관리실에서 생성된 초대권 코드를 목록으로 확인하고 수정/삭제할 수 있습니다. 제출 기록이 있는 코드는 삭제가 막힐 수 있으므로, 그 경우 `활성`을 해제하세요.

## 2. 아이템 수정/삭제

관리실에서 기존 아이템의 이름, 가격, 카테고리, 효과, 이미지, 정렬 순서, 판매 여부를 수정할 수 있습니다. 삭제 버튼도 추가했습니다. 구매/지급 기록이 있는 아이템은 삭제가 막힐 수 있으므로, 그 경우 판매중을 해제하세요.

## 3. 아이템 이미지 업로드

관리실에서 아이템 이미지를 파일로 업로드할 수 있습니다. 권장 크기는 아래와 같습니다.

```txt
800 × 600px
WebP/JPG/PNG
500KB 이하 권장
```

## 4. 관리실 테이블 정리

방문객 유형 컬럼 때문에 밀리던 관리실 테이블을 가로 스크롤 전제로 정리했습니다.

## 5. 상태 글씨 크기 조정

`괜 찮 아`, `조 심 해`, `위 험 해` 등의 상태 글씨를 조금 더 크게 조정했습니다.

## 6. v1.8 SQL 필요

아이템 이미지 업로드용 Storage 버킷 정책이 추가되므로 Supabase SQL Editor에서 아래 파일을 실행해야 합니다.

```txt
migrations/upgrade-v1.8.sql
```

---

# v1.9 변경사항

## 1. 관리실 표 정리

회원 목록, 초대권 코드, 아이템 관리 표에서 글자가 세로로 쪼개지지 않도록 수정했습니다. 표는 억지로 구겨 넣지 않고 가로 스크롤로 넓게 보이도록 정리했습니다.

회원 목록 컬럼은 아래 흐름으로 정리했습니다.

```txt
유쾌주화 / 상태 / 계정상태 / 권한 구분 / 권한설정 / 저장
```

## 2. 프로필 카드 문구 조정

오른쪽 프로필 카드의 `유쾌주화` 글씨 크기를 키우고, `판정 상태` 표기를 `방문객 상태`로 정리했습니다.

## 3. 디자인 연결감 조정

검은색/금색 로고 영역의 안쪽 메뉴 바탕은 검은색으로 유지하고, 본문은 기존 파스텔 기념품샵 느낌을 유지하도록 조정했습니다.

## 4. 다시 방문하기 메시지

`다시 방문하기`를 누르면 로그아웃 후 아래 문구가 팝업으로 표시됩니다.

```txt
이용해 주셔서 감사합니다, 고객님. 다음에 다시 방문해 주세요.
또 놀 러 와
```

## 5. 추가 SQL 없음

v1.8까지 SQL을 실행했다면 v1.9는 추가 SQL 실행이 필요 없습니다.

---

# v2.0 변경사항

## 1. 전체 디자인 검은색 + 금색 통일

상단 로고 영역뿐 아니라 본문, 카드, 버튼, 상점 카드도 검은색 + 금색 중심의 고급 기념품샵 톤으로 통일했습니다.

## 2. 상점 카드 구조 변경

상점 첫 화면에서는 아래 정보만 보입니다.

```txt
이미지
물품명
가격
자세히 / 구입
```

상세 설명은 `자세히` 버튼을 눌렀을 때 뜨는 팝업에서 확인합니다.

## 3. 특별관 문구 수정

가비아 마음결체 문장을 아래 문구로 바꾸고, 모바일에서도 박스 밖으로 튀어나가지 않도록 줄바꿈/overflow 처리를 강화했습니다.

```txt
안녕하십니까. 이 밤의 즐거움, 매일 만나는 새로운 얼굴. 그리고 친근한 당신의 사회자! 안녕하십니까. 여긴 심야 토크쇼입니다! 오늘의 게스트로 초대된 당신을 환영합니다.
```

## 4. 프로필 카드 상태 표시 수정

프로필 카드의 방문객 상태 칸에서는 `방문객 상태`라는 라벨 없이 판정 문구만 표시됩니다.

## 5. 추가 SQL 없음

v1.9까지 적용했다면 v2.0은 추가 SQL 실행이 필요 없습니다.

---

# v2.1 변경사항

## 1. 괴이 전용 상점 기초 구현

방문객 유형에 `오염자`와 `괴이`를 구분해서 사용할 수 있도록 확장했습니다.

- 일반 방문객: 일반 물품 표시
- 오염자: 오염자 전용 물품 표시
- 괴이: 인생 상품 표시
- 괴이가 인생을 구입하면 `가면 붕괴율`이 0으로 초기화됩니다.
- 괴이가 인생을 구입한 뒤에는 가면 붕괴율을 낮추는 가면 관리 물품이 추가로 보입니다.
- 현재 착용 중인 인생은 해당 괴이에게 `솔드아웃`으로 표시됩니다.

## 2. 추가 기본 물품

오염자 전용 물품과 괴이 전용 인생/가면관리 물품 샘플을 추가했습니다.

## 3. 상점 버튼 가로 정렬

`자세히 / 구입` 버튼이 세로로 쪼개지지 않고 한 줄에 보이도록 수정했습니다.

## 4. 프로필 상태 표시

프로필 카드의 상태 칸은 숫자와 판정이 위아래로 표시됩니다.

일반/오염자:
```txt
37
조 심 해
```

괴이:
```txt
—
측정 불필요
```

인생 구입 후 괴이:
```txt
12
가면 붕괴율
```

## 5. 특별관 괴이 전용 연출

괴이 계정으로 특별관에 들어가면 글리치 문구가 추가됩니다.

## 6. 관리실 아이템 UI 보정

아이템 이미지 URL과 파일 선택이 겹치지 않게 수정했고, 판매중 체크박스 위치도 정리했습니다.

## 7. SQL 실행 필요

이번 버전은 DB 컬럼과 구매 함수가 추가되므로 Supabase SQL Editor에서 아래 파일을 실행해야 합니다.

```txt
migrations/upgrade-v2.1.sql
```

---

# v2.2 변경사항

## 1. renderItems 오류 수정

v2.1에서 발생하던 `renderItems is not defined` 오류를 수정했습니다.  
상점 렌더링 코드를 안정적으로 다시 작성했습니다.

## 2. 아이템 이미지는 파일 업로드만 사용

관리실의 아이템 이미지 URL 입력칸을 제거했습니다.  
이제 아이템 이미지는 파일 선택으로만 등록/수정합니다.

## 3. 프로필 상태 표시 정리

프로필 카드의 상태 칸은 아래처럼 숫자와 판정이 위아래로 표시됩니다.

```txt
37
조 심 해
```

## 4. 오염도 100 계약서 이벤트

일반 방문객이 방문객 상태 100에 도달하면 표준 보안팀 근로계약서 팝업이 표시됩니다.  
`근로계약` 버튼을 누르면 방문객 유형이 `오염자`로 변경됩니다.

## 5. 다시 방문하기 팝업

`다시 방문하기` 버튼을 누르면 바로 로그아웃되지 않고 작별 팝업이 뜹니다.

```txt
이용해 주셔서 감사합니다, 방문객님!
다음에 다시 방문해 주세요!
또 놀 러 와
```

확인을 누르면 로그아웃됩니다.

## 6. 배경 흰색 잔상 제거

페이지 뒤쪽에 보이던 흰색 반투명 배경/상자 느낌을 제거하도록 CSS를 보강했습니다.

## 7. SQL 실행 필요

이번 버전은 오염도 100 계약서 처리를 위해 SQL 실행이 필요합니다.

```txt
migrations/upgrade-v2.2.sql
```

---

# v2.3 변경사항

## 1. 프로필 이미지 액박 수정

오른쪽 프로필 카드에서 프로필 이미지가 깨져 보이던 문제를 수정했습니다.  
프로필 이미지가 없으면 이미지 태그 대신 이름 앞글자 표시가 들어갑니다.

## 2. 상점 카드 배치 수정

상점 카드가 아래 구조로 보이도록 수정했습니다.

```txt
이미지
물품명
가격
자세히 / 구입
```

가격은 물품명 아래 왼쪽 정렬로 표시됩니다.

## 3. 프로필 상태 표시 정리

유쾌주화 칸과 방문객 상태 칸이 같은 형태로 보이도록 수정했습니다.

```txt
숫자
판정
```

판정 문구는 노란색/빨간색 볼드체가 강하게 적용됩니다.

## 4. 관리실 목록 복구

v2.2에서 관리실의 회원/초대권/아이템 목록이 뜨지 않던 문제를 수정했습니다.  
관리실 JS를 안정판으로 다시 작성했습니다.

## 5. 작별 문구 색상 보정

`또 놀 러 와` 문구를 더 쨍한 노란색으로 조정했습니다.

## 6. SQL 추가 없음

v2.2까지 적용했다면 v2.3은 SQL 추가 실행이 필요 없습니다.

---

# v2.4 변경사항

## 1. 관리실 이미지 파일 UI 정리

등록된 아이템 목록에서 `이미지 등록됨` 문구를 숨겼습니다. 파일 업로드는 파일 선택 입력만 보이도록 정리했습니다.

## 2. 프로필/상점 카드 UI 수정

- 프로필 닉네임 위치 조정
- 유쾌주화/상태 칸 크기 재조정
- 상태 판정 색상을 샛노란색/빨간색으로 강화
- 상점 가격을 오른쪽 정렬로 수정
- 자세히 팝업의 내부 스크롤을 없애고 이미지와 제목 사이 여백을 정리

## 3. 보안팀 근로계약서 수정

계약서의 을 이름이 현재 방문객 이름으로 표시됩니다. 계약서 문구를 백일몽 주식회사 보안팀 계약 내용으로 변경했습니다. 근로계약 후 방문객 상태가 0으로 초기화되고, 완료 팝업이 표시됩니다.

## 4. 괴이 화면 강화

괴이 계정은 전체 화면 톤이 더 직관적으로 달라지도록 청록색 글리치 계열 스타일을 추가했습니다.

## 5. 특별관 연출 변경

특별관 진입 시 `404 NOT FOUND` 연출이 먼저 표시됩니다. 일반 방문객과 괴이 방문객의 초대장 문구가 분리되었습니다.

## 6. 모바일 프로필 겹침 보정

모바일 화면에서 프로필 영역이 상품 목록을 가리지 않도록 프로필 영역의 sticky 성격을 해제했습니다.

## 7. SQL 실행 필요

이번 버전은 근로계약 후 방문객 상태 초기화를 위해 SQL 실행이 필요합니다.

```txt
migrations/upgrade-v2.4.sql
```

---

# v2.5 변경사항

## 1. 근로계약서 문구 보강

사용자가 제공한 백일몽 주식회사 보안팀 근로계약서 내용을 유지하면서, 근무 장소 이탈 금지와 불리한 계약 조항을 추가했습니다.

## 2. 특별관 연출 수정

일반/오염자 초대장은 404 연출 후 기존 문구가 점차 네모 문자로 변하고, 이후 `이리와` 문구와 `방청하기` 버튼이 표시됩니다.

괴이 초대장에서는 수신자 오류/이미 여기 있었다는 문구를 제거하고, 게스트 초대 문구만 표시합니다.

## 3. 관리실 페이지네이션 추가

회원, 아이템, 초대권 코드, 초대권 제출 내역, 비밀번호 요청 목록을 한 화면에 10개씩 볼 수 있게 했습니다.

## 4. 초대권 제출 내역 삭제 추가

테스트를 위해 초대권 제출/승인/거절 내역을 관리실에서 삭제할 수 있게 했습니다.
삭제 후 같은 사용자가 같은 코드를 다시 등록할 수 있습니다.

## 5. SQL 실행 필요

이번 버전은 초대권 제출 내역 삭제 함수가 추가되므로 SQL 실행이 필요합니다.

```txt
migrations/upgrade-v2.5.sql
```

---

# v2.6 변경사항

## 1. 프로필 수치 박스 축소

오른쪽 프로필 카드의 유쾌주화/상태 수치 박스와 글자 크기를 조금 줄였습니다.

## 2. 초대장 이리와 문구 수정

초대장 애니메이션의 `이리와` 문구를 가비아체/노란색이 아니라 버튼에 가까운 기본 글씨체 + 빨간색으로 표시하도록 조정했습니다.

## 3. 근로계약서 스크롤 정리

근로계약서의 내부 스크롤을 없애고, 계약서 팝업 자체에만 검은색/황금색 스크롤이 생기도록 정리했습니다.

## 4. 괴이 모드 색상 통일

괴이 모드에서 남아 있던 금색 계열을 더 줄이고, 청록색/검은색 계열로 통일했습니다.

## 5. SQL 추가 없음

v2.5까지 적용했다면 v2.6은 SQL 실행이 필요 없습니다.

---

# v2.7 변경사항

## 1. 일반/오염자 초대장 이리와 문구 수정

초대장 애니메이션의 `이리와` 문구가 가비아체나 노란색 강조문처럼 보이지 않도록 수정했습니다.  
이제 경고문과 같은 기본 글씨체/크기 계열의 빨간 글씨로 화면을 채웁니다.

## 2. 모바일 크기 재조정

v2.6에서 모바일 프로필 카드와 상품 카드가 과하게 작아진 부분을 다시 키웠습니다.  
모바일에서도 화면에 맞되 너무 쪼그라들지 않도록 조정했습니다.

## 3. SQL 추가 없음

v2.7은 CSS/연출 수정판이라 SQL 실행이 필요 없습니다.

---

# v2.8 변경사항

## 1. 초대장 이리와 스타일 재수정

일반/오염자 초대장의 `이리와` 문구가 가비아체/노란색 강조문처럼 남아 보이던 문제를 다시 강하게 수정했습니다.  
이제 경고문과 같은 계열의 빨간 기본 글씨체로 표시됩니다.

## 2. 쇼핑백 빈 상태 상자 조정

`보유 아이템이 없습니다` 상자가 다른 화면과 어우러지도록 검은색/금색 계열로 낮췄습니다.

## 3. 괴이 모드 색상 강제 통일

괴이 모드에서 남아 있던 금색 메뉴, 프로필, 아이템 이미지 선, 안내 메시지 색상을 청록/검은색 계열로 통일했습니다.

## 4. 괴이 전용 메뉴명

괴이 계정에서는 상점 카테고리가 다음처럼 보입니다.

```txt
전체 / 인생 진열장 / 가면 수선소 / ■■ ■■ / 특별관
```

일반/오염자는 `분실물` 대신 `스낵`으로 보입니다.

## 5. 프로필 수치 박스 재축소

유쾌주화와 상태 수치 박스가 아직 과하게 커 보이던 문제를 다시 줄였습니다.

## 6. 괴이 가면 붕괴율 판정

괴이는 `가면 붕괴율`이라는 라벨 대신 수치에 따라 아래 판정을 표시합니다.

```txt
가면 안정 / 틈새 발생 / 위장 불안정 / 식별 임박 / 가면 붕괴
```

## 7. SQL 추가 없음

v2.8은 JS/CSS 수정판이라 SQL 실행이 필요 없습니다.

---

# v2.9 변경사항

## 1. 모바일 프로필 폭 수정

모바일에서 상품 목록은 넓게 보이는데 프로필 카드만 좁아 보이던 문제를 수정했습니다.  
프로필 카드가 모바일에서도 컨테이너 폭을 제대로 사용하도록 조정했습니다.

## 2. 초대장 애니메이션 순서 수정

일반/오염자 초대장 연출을 아래 순서로 수정했습니다.

```txt
404 NOT FOUND
→ ■ 문구가 끝까지 출력
→ 이리와 문구가 끝까지 출력
→ 심야토크쇼의 방청객으로 초대되셨습니다.
→ 방청하기 버튼 표시
```

이제 ■가 끝나기 전에 이리와가 끊겨 보이는 문제가 없어야 합니다.

## 3. 초대장 이리와 스타일 재강제

`이리와` 문구가 가비아체/노란색으로 남아 보이던 문제를 다시 강하게 수정했습니다.  
빨간색 기본 글씨체로 표시되도록 별도 애니메이션 구조와 CSS를 적용했습니다.

## 4. 괴이 모드 색상 전역 적용 강화

괴이 모드에서 카테고리 메뉴, 쇼핑백/방문객 정보/초대권 등록 버튼, 작별 팝업 일부가 금색으로 남던 문제를 보정했습니다.  
html과 body 양쪽에 entity-mode 클래스를 적용하도록 수정했습니다.

## 5. 프로필 수치 박스 정렬 수정

유쾌주화/상태 수치가 네모 박스 위쪽에 붙어 보이던 문제를 수정하고 중앙 정렬을 강화했습니다.

## 6. SQL 추가 없음

v2.9는 JS/CSS 수정판이라 SQL 실행이 필요 없습니다.

---

# v3.0 변경사항

## 1. 초대장 최종 문구 스타일 수정

`심야토크쇼의 방청객으로 초대되셨습니다.` 문구가 혼자 흰색/큰 글씨/굵은 글씨로 튀던 문제를 수정했습니다.  
이제 초대장 경고문과 같은 빨간 기본 글씨체 계열로 표시됩니다.

## 2. 수신 완료 문구

일반/오염자 초대장에서 `방청하기` 버튼까지 표시되면 상단 설명이 `수신이 완료되었습니다.`로 바뀝니다.

## 3. 괴이 초대장 애니메이션

괴이 초대장의 문구도 한 번에 뜨지 않고 한 글자씩 출력되도록 수정했습니다.

## 4. 작별 팝업 색상

일반/오염자/괴이 모드 모두 `또 놀 러 와`는 쨍한 노란색으로 고정했습니다.  
괴이 모드에서도 청록색으로 바뀌지 않게 했습니다.

## 5. 404 NOT FOUND 색상

괴이 모드에서도 404 NOT FOUND는 붉은색으로 유지되도록 수정했습니다.

## 6. 괴이 모드 색상 강제 강화

카테고리 메뉴와 프로필 사이드 버튼이 금색으로 남던 문제를 더 강한 선택자로 수정했습니다.

## 7. 괴이 모드 게이지

괴이 모드의 상태 게이지가 붉은색으로 차오르도록 조정했습니다.

## 8. 프로필 수치 박스 중앙 정렬

유쾌주화/상태 수치가 상자 위쪽에 붙어 보이던 문제를 수정하고 중앙 정렬을 강화했습니다.

## 9. SQL 추가 없음

v3.0은 JS/CSS 수정판이라 SQL 실행이 필요 없습니다.

---

# v3.1 변경사항

## 1. 카테고리명 전역 적용

일반/오염자 모드에서는 모든 페이지에서 `분실물` 대신 `스낵`으로 표시됩니다.  
괴이 모드에서는 모든 페이지에서 아래 메뉴명이 적용됩니다.

```txt
전체 / 인생 진열장 / 가면 수선소 / ■■ ■■ / 특별관
```

## 2. 괴이 모드 잔여 금색 수정

쇼핑백 빈 상태, 카테고리 위아래 선, placeholder, 프로필 이미지, 관리자 페이지네이션 등 남아 있던 금색 계열을 청록색 계열로 수정했습니다.

## 3. 기념품샵 떠나기 영역 유지

괴이 모드에서도 `기념품샵 떠나기` 영역과 설명은 붉은색으로 유지됩니다.

## 4. 괴이 초대장 문구 색상

괴이 초대장의 타이핑 문구는 청록색이 아니라 붉은색으로 표시됩니다.

## 5. 프로필 수치 박스 중앙 정렬

유쾌주화/상태 수치 박스의 글자가 가로/세로 모두 가운데 오도록 다시 조정했습니다.

## 6. 괴이 방문객 정보 게이지

괴이 모드에서 방문객 정보 페이지의 상태 게이지가 가면 붕괴율 기준으로 움직이고, 붉은색으로 표시되도록 수정했습니다.

## 7. 괴이 관리자 조정 로직

관리자가 괴이의 방문객 상태 변화량을 조정하면 `pollution`이 아니라 `mask_collapse_rate`가 변경되도록 SQL 함수를 수정했습니다.  
초대권 승인 시 방문객 상태 변화량도 괴이에게는 가면 붕괴율로 반영됩니다.

## 8. SQL 실행 필요

이번 버전은 괴이 관리자 조정 로직 변경 때문에 SQL 실행이 필요합니다.

```txt
migrations/upgrade-v3.1.sql
```

---

# v3.2 변경사항

## 1. 초대장 마지막 문구 정렬

`심야토크쇼의 방청객으로 초대되셨습니다.` 문구를 가운데 정렬하고, `방청하기` 버튼 바로 위에 붙도록 조정했습니다.

## 2. 프로필 이미지 표시 보정

프로필 이미지가 없을 때 앞글자 두 글자를 표시하는 기준을 `밴드 닉네임 → 사용자 이름 → ?` 순서로 바꿨습니다.  
방문객 정보에서 밴드 닉네임을 저장하면 상점 오른쪽 프로필 카드에도 반영됩니다.

## 3. 프로필 이미지 삭제

방문객 정보에 `프로필 이미지 삭제` 버튼을 추가했습니다.  
삭제 예약 후 저장을 누르면 기존 프로필 이미지 연결이 해제됩니다.

## 4. 프로필 수치 카드 위치 조정

상점 오른쪽 프로필 카드와 방문객 정보 페이지의 유쾌주화/상태 수치 카드 위치를 다시 조정했습니다.

## 5. 근로계약서 명칭 수정

근로계약서의 갑을 `청 이사`에서 `청달래`로 변경했습니다.

## 6. 괴이 동기화 판정 변경

괴이의 가면 붕괴율 판정 문구를 아래처럼 변경했습니다.

```txt
0~39    동기화 안정적
40~75   동기화 불안정
76~99   동기화 해제 위험
100     동기화가 해제되었습니다.
```

## 7. 동기화 100 도달 처리

괴이의 동기화 수치가 100에 도달하면 가면이 깨지는 연출 후 현재 착용 중인 인생이 해제됩니다.  
이후 다시 인생을 구입하기 전까지는 `측정 불필요` 상태로 돌아갑니다.

## 8. 인생 해제 아이템 추가

괴이 전용 특별 상품으로 `물거품 귀환권`을 추가했습니다.  
이 물품을 구매하면 현재 착용 중인 인생이 해제되고 측정 불필요 상태로 돌아갑니다.

## 9. 인생 재구입 초기화

인생을 다시 구입하면 동기화 수치는 0으로 초기화됩니다.

## 10. 로그아웃 후 괴이 CSS 잔류 수정

괴이 계정에서 로그아웃하면 일반 모드 CSS로 돌아가도록 localStorage 모드를 초기화합니다.

## 11. 스크롤바 디자인

기본/오염자/괴이 모드별로 스크롤바 색상을 다르게 조정했습니다.

## 12. SQL 실행 필요

이번 버전은 괴이 인생 해제 로직과 물거품 귀환권 추가 때문에 SQL 실행이 필요합니다.

```txt
migrations/upgrade-v3.2.sql
```

---

# v3.3 변경사항

## 1. 일반/오염자 특별관 인트로 추가

404 NOT FOUND 이후 일반/오염자 특별관에 아래 연출을 추가했습니다.

```txt
보라!
→ 선언문 타이핑
→ 나는 누구야? 반복 타이핑
→ 너는 누구야?
→ 초대장 본문 연출
```

그 뒤 기존처럼 `■` 문구와 `이리와` 문구가 순서대로 출력되고, 방청하기 버튼이 표시됩니다.

## 2. 가면 파손 연출 수정

동기화 100 도달 시 뜨는 파손 연출을 더 가면처럼 보이도록 수정했습니다.  
알처럼 보이던 조각형 연출 대신 눈구멍과 균열이 있는 가면 실루엣으로 바꿨습니다.

## 3. 가면 파손 모달 확인 방식

가면 파손 모달이 자동으로 너무 빨리 사라지지 않게 수정했습니다.  
이제 사용자가 내용을 읽고 `확인`을 눌러야 인생 해제 처리가 진행됩니다.

## 4. 가면 파손 문구 붉은색 유지

가면 파손 모달 안의 문구와 버튼을 붉은색으로 유지하도록 수정했습니다.

## 5. 괴이 프로필 기본 이미지 색상

괴이 모드에서 프로필 이미지가 없을 때 뜨는 앞글자 기본 이미지가 노란색으로 남던 문제를 수정했습니다.  
이제 청록색 계열로 표시됩니다.

## 6. 괴이 모드 로고/카테고리 선 색상 보정

괴이 모드에서 로고 아래, 카테고리 위쪽의 얇은 금색선이 남는 문제를 추가로 보정했습니다.

## 7. SQL 추가 없음

v3.3은 JS/CSS 수정판이라 SQL 실행이 필요 없습니다.

---

# v3.4 변경사항

## 1. 프로필 수치 박스 정렬 재수정

상점 오른쪽 프로필 카드의 유쾌주화/상태 박스 안 글자가 위쪽에 붙어 보이던 문제를 다시 수정했습니다.  
박스 자체 높이를 고정하고, 내부 글자를 가로/세로 가운데 정렬하도록 강제했습니다.

## 2. 특별관 인트로 전체화면 컷 전환

일반/오염자 특별관 연출을 카드 안에서 진행하지 않고, 404 NOT FOUND처럼 전체 검은 화면에서 진행하도록 바꿨습니다.

```txt
404 NOT FOUND
→ 검은 화면
→ 보라!
→ 검은 화면
→ 선언문 타이핑
→ 검은 화면
→ 나는 누구야? 타이핑
→ 검은 화면
→ 너는 누구야?
→ 특별관 카드 화면
→ ■ 출력
→ 이리와 출력
→ 방청하기
```

## 3. 괴이 기본 프로필 글자색 보정

괴이 모드에서 프로필 이미지가 없을 때 앞글자 기본 이미지가 노란색으로 남던 문제를 추가로 보정했습니다.  
텍스트 페인트 색까지 청록색으로 강제했습니다.

## 4. 괴이 모드 카테고리 선 추가 보정

괴이 모드에서 카테고리 위쪽 금색선이 남는 문제를 header/category-strip 계열까지 추가로 덮었습니다.

## 5. SQL 추가 없음

v3.4는 JS/CSS 수정판이라 SQL 실행이 필요 없습니다.

---

# v3.5 변경사항

## 1. 특별관 인트로를 팝업형 컷 전환으로 재수정

일반/오염자 특별관 연출을 다시 수정했습니다.  
404 NOT FOUND 이후 전체 검은 팝업 화면에서 각 문구가 하나씩 뜨고 사라집니다.

```txt
404 NOT FOUND
→ 검은 팝업: 보라!
→ 사라짐
→ 검은 팝업: 선언문 타이핑
→ 사라짐
→ 검은 팝업: 나는 누구야? 타이핑
→ 사라짐
→ 검은 팝업: 너는 누구야?
→ 사라짐
→ 특별관 카드 화면
→ 본래 초대장 문구 5초 노출
→ ■ 문구 출력
→ 이리와 출력
→ 방청하기
```

## 2. 본래 초대장 문구 노출

특별관 카드 화면이 보인 뒤 본래 초대장 문구가 약 5초간 표시됩니다.  
그 뒤 기존처럼 `■` 문구와 `이리와` 문구로 이어집니다.

## 3. 프로필 수치 박스 정렬 재강화

상점 오른쪽 프로필 카드의 유쾌주화/상태 박스 내부 글자를 다시 강하게 중앙 정렬했습니다.

## 4. 괴이 프로필 앞글자 색상 추가 보정

괴이 모드에서 기본 프로필 앞글자가 금색으로 남는 문제를 추가로 보정했습니다.

## 5. 괴이 카테고리 선 추가 보정

괴이 모드 카테고리 선에 남는 금색 배경/선 계열을 추가로 덮었습니다.

## 6. SQL 추가 없음

v3.5는 JS/CSS 수정판이라 SQL 실행이 필요 없습니다.

---

# v3.6 변경사항

## 1. 특별관 팝업 인트로 글씨체 변경

404 이후 뜨는 `보라!`, 선언문, `나는 누구야?`, `너는 누구야?`의 글씨체를 본명조/명조 계열로 변경했습니다.

## 2. 본래 초대장 문구 수정

`■`로 바뀌기 전 5초간 표시되는 본래 초대장 문구를 아래 문장으로 수정했습니다.

```txt
단, 이 버튼을 누름으로써 발생하는 공포, 환청, ■■, ■■■■ 등의 문제에 대하여 골든 리조트는 일체의 배상책임이 없습니다.
우 리 같 이 놀 자
안녕하십니까! 이 밤의 즐거움. 매일 만나는 새로운 얼굴. 그리고 친근한 당신의 사회자! 안녕하십니까. 여기는 심야 토크쇼입니다! 오늘의 방청객으로 초대된 것을 환영합니다.
```

## 3. 초대장 오염 전환 연출 수정

본래 초대장 문구가 5초간 보인 뒤, 같은 영역이 붉은 기본 글씨체로 흔들리며 오염되고, `■` 문구가 한 글자씩 출력됩니다.  
그 뒤 기존처럼 `이리와` 문구와 방청하기 버튼으로 이어집니다.

## 4. SQL 추가 없음

v3.6은 JS/CSS 수정판이라 SQL 실행이 필요 없습니다.

---

# v3.7 변경사항

## 1. 특별관 팝업 인트로 수정

`보라!`부터 `너는 누구야?`까지의 팝업형 검은 화면에서 빨간 네모 박스를 제거했습니다.  
글씨체는 명조/본명조 계열로 바꾸고 이탤릭 처리를 추가했습니다.

## 2. 본래 초대장 색상 보정

`우 리 같 이 놀 자`는 노란색으로, 사회자 초대 문구는 흰색/가비아체로 유지되도록 다시 보정했습니다.

## 3. 초대장 오염 전환 보정

본래 초대장 문구가 5초간 표시된 뒤, 같은 영역이 붉은 기본 글씨체로 오염되고 `■` 문구가 출력됩니다.  
그 뒤 `이리와` 문구와 방청하기 버튼으로 이어집니다.

## 4. 근로계약서 을 이름 기준 변경

근로계약서의 `을`은 밴드 닉네임이 아니라 사용자 이름으로 표시됩니다.

## 5. 오염자 계약 해제 아이템 추가

오염자 전용 특별 상품으로 `황금 해약서`를 추가했습니다.  
구매하면 청달래와 강제 체결된 보안팀 근로계약이 파기되고, 일반 방문객으로 돌아갑니다.  
오염도는 0으로 초기화됩니다.

## 6. 계약 파기 연출 추가

`황금 해약서` 구매 시 신성한 금색/하얀색 팝업이 뜨며, 계약서가 찢어지는 연출과 함께 아래 문구가 표시됩니다.

```txt
당신을 구속하고 있던 근로계약서의 힘이 사라집니다.
```

## 7. SQL 실행 필요

이번 버전은 오염자 계약 해제 아이템과 구매 로직이 추가되어 SQL 실행이 필요합니다.

```txt
migrations/upgrade-v3.7.sql
```

---

# v3.8 변경사항

## 1. 초대장 오염 문구 수정

본래 초대장 문구가 흔들린 뒤 아래 형태의 여러 줄 빨간 `■` 문장으로 바뀌도록 수정했습니다.

```txt
■, 이 ■■■ ■■■■■ ■■■■ ■■, ■■, ■■, ■■■■ ■■ ■■■ ■■■ ■■ 리■■■ ■■■ ■■■■와 ■■■■.
■ ■ ■ ■ ■ ■
■■■■■■! ■ ■■ ■■■. 매일 ■■■ ■■■ ■■. ■■■ ■■■ ■■■ ■■■! ■■■■■■. 여기■ ■■ ■■■■■■! ■■■ ■영■■■ ■■원 ■히 환영합니다.
```

그 뒤 `이리와` 문구가 출력됩니다.

## 2. 가면 파손 문구 수정

동기화 100 도달 시 팝업 문구를 아래처럼 변경했습니다.

```txt
가면이 깨졌습니다.

당신의 존재감이 커져 더 이상 이 가면을 사용하기 어렵습니다. 본래 당신의 모습으로 되돌아갑니다.
```

## 3. 가면 파손 색상/연출 보강

가면 파손 팝업의 글자색을 모두 붉은색 볼드체로 고정했습니다.  
가면이 더 잘게 조각나는 느낌이 나도록 조각 애니메이션을 추가했습니다.

## 4. 황금 해약서 팝업 보강

황금 해약서 팝업을 흰색 바탕 + 찬란한 황금색 글씨로 수정했습니다.  
근로계약서처럼 보이는 종이가 좌우로 쫙쫙 찢어지는 느낌을 더 강화했습니다.

## 5. SQL 추가 없음

v3.8은 JS/CSS 수정판이라 SQL 실행이 필요 없습니다.

---

# v3.9 변경사항

## 1. 특별관 멈춤 오류 수정

일반/오염자 특별관에서 `수신 중입니다.`만 뜨고 연출이 진행되지 않던 문제를 수정했습니다.  
여러 줄 `■` 문장을 배열 기반으로 출력하도록 바꿔 JS 문자열 오류 가능성을 제거했습니다.

## 2. 가면 파손 팝업 테두리 수정

가면 파손 팝업 주변 테두리가 청록색으로 남던 문제를 수정했습니다.  
테두리/외곽선/그림자를 붉은색으로 고정했습니다.

## 3. 가면 파손 애니메이션 보강

가면이 먼저 떠 있다가 조각들이 아래로 떨어지는 느낌이 나도록 애니메이션을 다시 보강했습니다.

## 4. 황금 해약서 팝업 표시 수정

황금 해약서 구매 후 계약서 찢어짐 팝업이 뜨지 않던 문제를 수정했습니다.  
구매 결과값뿐 아니라 아이템 종류/메시지 기준으로도 팝업을 띄우도록 보정했습니다.

## 5. SQL 추가 없음

v3.9는 JS/CSS 수정판이라 SQL 실행이 필요 없습니다.

---

# v4.0 변경사항

## 1. 초대장 오염 연출 수정

본래 있던 초대장 문구가 사라지고 새 `■` 문장이 타이핑되는 방식이 아니라,  
원래 글자들이 그 자리에서 하나씩 `■` 또는 오염된 문자로 바뀌도록 수정했습니다.

## 2. 가면 훼손 팝업 문구 수정

`가면이 깨졌습니다.`를 `가면이 훼손됐습니다.`로 변경했습니다.

## 3. 가면 훼손 버튼 테두리 수정

확인 버튼의 청록색 테두리를 붉은색으로 고정했습니다.

## 4. 가면 훼손 애니메이션 수정

가면이 먼저 떠 있다가 조각들이 아래로 떨어지는 느낌이 더 나도록 수정했습니다.  
조각들은 제목 위쪽에서 떨어지는 형태로 보이게 조정했습니다.

## 5. 황금 해약서 팝업 표시 보정

황금 해약서 구매 후 메시지만 뜨고 팝업이 뜨지 않던 문제를 다시 보정했습니다.  
팝업을 최상단으로 띄우고, 계약서 조각 애니메이션을 추가했습니다.

## 6. SQL 추가 없음

v4.0은 JS/CSS 수정판이라 SQL 실행이 필요 없습니다.

---

# v4.1 변경사항

## 1. 특별관 멈춤 오류 재수정

특별관에서 `수신 중입니다.`만 뜨고 멈추던 JS 오류를 수정했습니다.  
문자열 내부 줄바꿈 처리 문제를 제거하고, 초대장 원문 글자가 그 자리에서 하나씩 오염되도록 다시 작성했습니다.

## 2. 황금 해약서 팝업 강제 표시

황금 해약서 구매 시 메시지만 뜨고 별도 팝업이 뜨지 않던 문제를 수정했습니다.  
구매 직후 가면 훼손 팝업처럼 별도 전체화면 팝업이 뜨도록 처리했습니다.

## 3. 가면 훼손 애니메이션 재수정

가면 본체와 상처는 깨진 뒤 사라지고, 조각들만 아래로 떨어져 남도록 애니메이션을 수정했습니다.

## 4. 가면 훼손 버튼 테두리 수정

확인 버튼의 청록색 테두리를 붉은색으로 고정했습니다.

## 5. SQL 추가 없음

v4.1은 JS/CSS 수정판이라 SQL 실행이 필요 없습니다.

---

# v4.2 변경사항

## 1. 프로필 수치 박스 위치 조정

상점 오른쪽 프로필 카드의 유쾌주화/방문객 상태 박스 안 글자들이 위쪽에 붙어 보이던 문제를 조정했습니다.  
내부 글자들을 아래쪽으로 강제 이동했습니다.

## 2. 첫 로그인 초대장 안내 팝업 추가

처음 로그인한 방문객에게 초대장 안내 팝업이 뜹니다.

```txt
방문객님을 환영합니다.
당신에게 초대장이 도착했습니다.
특별관을 확인해 주세요!
```

## 3. 편지 봉투 애니메이션 추가

첫 로그인 안내 팝업에는 검은색+금색 테마의 편지 봉투 애니메이션이 표시됩니다.  
편지가 날아와 봉투가 열리는 느낌으로 구성했습니다.

## 4. 다시 뜨지 않도록 저장

초대장 안내 팝업은 사용자별로 한 번만 표시되며, 확인을 누르면 localStorage에 저장됩니다.

## 5. SQL 추가 없음

v4.2는 JS/CSS 수정판이라 SQL 실행이 필요 없습니다.

---

# v4.3 변경사항

## 1. 초대장 안내 팝업 표시 수정

첫 로그인 초대장 안내 팝업이 뜨지 않던 문제를 수정했습니다.  
프로필 로드 후 0.8초 뒤 강제로 표시되도록 변경했습니다.

## 2. 안내 팝업 저장 키 변경

기존 브라우저 localStorage에 값이 남아 있어도 다시 확인할 수 있도록 저장 키를 v4.3 기준으로 변경했습니다.

```txt
golden_invite_notice_seen_v43_사용자ID
```

## 3. 안내 팝업 z-index/display 강화

다른 팝업이나 화면 요소에 묻히지 않도록 z-index와 display 규칙을 강화했습니다.

## 4. SQL 추가 없음

v4.3은 JS/CSS 수정판이라 SQL 실행이 필요 없습니다.

---

# v4.4 변경사항

## 1. 초대장 안내 팝업 기준 변경

첫 로그인 여부가 아니라, 해당 계정으로 특별관을 확인했는지 여부를 기준으로 안내 팝업을 표시합니다.

## 2. 특별관 미확인 계정 안내

로그인한 계정이 아직 특별관에 들어간 적이 없으면 상점 화면에서 안내 팝업이 뜹니다.

```txt
방문객님을 환영합니다.
당신에게 초대장이 도착했습니다.
특별관을 확인해 주세요!
```

## 3. 특별관 확인 처리

특별관 페이지에 들어가면 브라우저 localStorage에 아래 값이 저장됩니다.

```txt
special_hall_seen_v44_사용자ID = 1
```

그 뒤부터는 해당 브라우저에서 같은 계정으로 로그인해도 초대장 안내 팝업이 뜨지 않습니다.

## 4. 나중에 확인 버튼

`나중에 확인`을 누르면 현재 브라우저 세션 동안만 팝업을 닫습니다.  
특별관을 실제로 확인하지 않으면 다음 로그인/새 세션에서 다시 뜰 수 있습니다.

## 5. SQL 추가 없음

v4.4는 JS/CSS 수정판이라 SQL 실행이 필요 없습니다.

---

# v4.5 변경사항

## 1. `나중에 확인` 동작 수정

초대장 안내 팝업에서 `나중에 확인`을 누르면 이제 현재 페이지에서만 닫힙니다.  
로그아웃 후 다시 로그인하거나, 페이지를 새로 열면 특별관 미확인 계정에는 다시 팝업이 뜹니다.

## 2. 특별관 확인 완료 조건 유지

팝업이 영구적으로 뜨지 않게 되는 조건은 오직 특별관 페이지에 실제로 들어간 경우입니다.

```txt
special_hall_seen_v45_사용자ID = 1
```

기존 v4.4 확인 기록도 같이 인정합니다.

## 3. SQL 추가 없음

v4.5는 JS/CSS 수정판이라 SQL 실행이 필요 없습니다.

---

# v4.6 변경사항

## 1. 브랜드명 변경

기존 `마스코트 골든의 기념품샵` / `마스코트 골든의 기념품샵` 계열 명칭을 아래 명칭으로 변경했습니다.

```txt
마스코트 골든의 기념품샵
```

## 2. 영어 표기 변경

영문 표기는 아래처럼 변경했습니다.

```txt
Mascot Golden's Souvenir Shop
```

## 3. 적용 범위

HTML title, 로고/헤더, 안내문, JS 내부 문구, README, VERSION 등 텍스트 파일 전반의 명칭을 일괄 교체했습니다.

## 4. 유지한 명칭

심야 토크쇼 면책 문구의 `골든 리조트`는 별도 세계관 명칭으로 보고 유지했습니다.

## 5. SQL 추가 없음

v4.6은 명칭/문구 수정판이라 SQL 실행이 필요 없습니다.

---

# v4.7 변경사항

## 1. 아이템 설명 수정 추가

관리실의 `등록된 아이템` 목록에서 기존 아이템의 설명을 수정할 수 있도록 설명 입력칸을 추가했습니다.

## 2. 효과 타입 입력란 제거

아이템 등록/수정 화면에서 `효과 타입` 입력란을 제거했습니다.  
효과 타입은 운영자가 직접 수정할 값이 아니라 내부 처리용 값입니다.

## 3. 효과 타입 자동 지정

아이템의 `물품 구분`에 따라 효과 타입이 자동으로 저장됩니다.

```txt
일반 → pollution_delta
인생 → entity_life
가면관리 → mask_collapse_delta
인생해제 → life_cancel
계약해제 → contract_release
```

## 4. 효과값은 유지

효과값은 계속 수정할 수 있습니다.  
특히 가면관리 아이템은 효과값으로 동기화 수치를 조정하므로 유지했습니다.

## 5. SQL 추가 없음

v4.7은 관리실 UI/JS 수정판이라 SQL 실행이 필요 없습니다.

---

# v4.8 변경사항

## 1. 괴이 모드 파일 선택 색상 수정

관리실의 아이템 이미지 파일 선택 input에서 `선택된 파일 없음` 문구가 금색으로 남던 문제를 수정했습니다.  
괴이 모드에서는 파일 선택 영역과 파일 선택 버튼이 청록색 계열로 표시됩니다.

## 2. 모드별 파일 선택 버튼 색상 통일

파일 선택 버튼을 방문객 모드별 색상에 맞게 조정했습니다.

```txt
일반 → 금색
오염자 → 붉은색
괴이 → 청록색
```

## 3. 괴이 모드 설명 수정칸 테두리 수정

관리실의 아이템 설명 수정 textarea 테두리가 금색으로 남던 문제를 수정했습니다.  
괴이 모드에서는 청록색 테두리로 표시됩니다.

## 4. 아이템 이미지 잘림 수정

아이템 이미지가 위아래로 잘리던 문제를 줄이기 위해 상품 카드/상세 팝업 이미지 표시 방식을 `contain`으로 변경했습니다.  
800x600 이미지가 잘리지 않고 전체가 보이도록 조정했습니다.

## 5. SQL 추가 없음

v4.8은 CSS 수정판이라 SQL 실행이 필요 없습니다.

---

# v4.9 변경사항

## 1. 브랜드명 재수정

일부 화면에서 `마스코트 골든의 기념품샵` / `Mascot Golden` 계열 문구가 다시 보일 수 있는 문제를 방지하기 위해 전체 텍스트 파일을 다시 검색/치환했습니다.

```txt
마스코트 골든의 기념품샵
Mascot Golden's Souvenir Shop
```

## 2. 특별관 문구 중앙 정렬

특별관 인트로의 `너는 누구야?` 문구와 `이리와` 문구를 중앙 정렬로 고정했습니다.

## 3. SQL 추가 없음

v4.9는 텍스트/CSS 수정판이라 SQL 실행이 필요 없습니다.

---

# v5.0 변경사항

## 1. 주요 파일 강제 덮어쓰기용 버전 표식 추가

GitHub 업로드 후 일부 파일만 바뀐 것처럼 보이는 문제를 줄이기 위해 주요 HTML/JS/CSS 파일에 `v5.0` 버전 표식을 추가했습니다.  
이렇게 하면 GitHub가 해당 파일들을 변경된 파일로 인식하기 쉬워집니다.

## 2. 브랜드명 재수정

전체 텍스트 파일에서 예전 브랜드명을 다시 검색/치환했습니다.

```txt
마스코트 골든의 기념품샵
Mascot Golden's Souvenir Shop
```

## 3. 특별관 중앙 정렬 보강

`너는 누구야?`와 `이리와` 문구를 부모 컨테이너까지 포함해 중앙 정렬로 강제했습니다.

## 4. PC 이미지 잘림 수정 보강

상품 카드와 상세 팝업의 이미지 컨테이너와 이미지 태그 모두에 4:3 비율과 `object-fit: contain`을 적용했습니다.  
PC 화면에서도 800x600 이미지가 잘리지 않고 전체 표시되도록 보강했습니다.

## 5. SQL 추가 없음

v5.0은 HTML/CSS/JS 수정판이라 SQL 실행이 필요 없습니다.
